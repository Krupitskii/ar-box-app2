import React from 'react'
import ReactDOM from 'react-dom/client'
import OracleCube from './OracleCube.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OracleCube />
  </React.StrictMode>,
)
